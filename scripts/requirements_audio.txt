# Requirements for generating audio files
# Cài đặt: pip install -r scripts/requirements_audio.txt

# Option 1: Microsoft Edge TTS (khuyến nghị - chất lượng tốt, miễn phí)
edge-tts>=6.1.0

# Option 2: Google TTS (cần internet)
gtts>=2.5.0
pydub>=0.25.1

# BGM + TTS: scripts/generate_audio.py (bgm / tts / bundle) + scripts/bgm/screen_bgm_presets.json
numpy>=1.22.0
scipy>=1.8.0

